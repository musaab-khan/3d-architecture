250 subtracted from the viewport3D in object creation and useEffect

added the way to create the 2d for a cube (box in canvas utils, need to make the cases for showing)

add a component that displays object dimensions in input boxes and on change there should be a function that changes the object values. Use this:
    import React, { useState } from 'react';

const InputExample = () => {
  const [inputValue, setInputValue] = useState('');

  const handleChange = (event) => {
    setInputValue(event.target.value);
    console.log('Input changed:', event.target.value); // Perform action here
  };

  return (
    <div>
      <input 
        type="text" 
        value={inputValue} 
        onChange={handleChange} 
        placeholder="Type something..."
      />
      <p>Current Value: {inputValue}</p>
    </div>
  );
};

export default InputExample;
